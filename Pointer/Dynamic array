#include <iostream>
using namespace std;

int main()
{
    int *p,i,n;
    cout<<"Enter your number array list : ";
    cin>>n;
    p=(int *)malloc(n*sizeof(int));
    cout<<"Enter your number in the array :"<<endl;
    for(i=0;i<n;i++)
    cin>>p[i];
    for(i=0;i<n;i++)
    cout<<p[i]<<" ";
}
